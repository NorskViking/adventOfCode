import { readInput } from "../../utils/helpers";
import { readLines } from "../../utils/helpers";

// Read input files
const puzzle_data: string = readInput('./test_input.txt');
const puzzle_lines: string[] = readLines(puzzle_data);

const rotation_direction: string[] = [];
const rotation_number: number[] = [];

// Find direction and number
for (const line of puzzle_data) {
    
}

const solve_roation_puzzle = () => {};